# frontend/.env.example
#
# Phase 0 status: no environment variables are required by the frontend.
# The MongoDB-backed /api/transactions route (Mongoose) has been removed
# along with its MONGO_URI dependency. See docs/SMARTCA-REPOSITORY-AUDIT.md
# and MIGRATION-CHECKPOINT.md for details.
#
# Phase 1 (future — do not add yet):
#   DATABASE_URL=   # PostgreSQL connection string, introduced with Drizzle
